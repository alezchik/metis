{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://metis.xmartlabs.com/schemas/risk.schema.json",
  "title": "Risk entry frontmatter",
  "description": "Frontmatter de una entrada knowledge/risks/*.md.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "type", "status", "title", "date", "severity", "evidence", "confidence"],
  "properties": {
    "id": { "type": "string", "pattern": "^RISK-[0-9]{4}$" },
    "type": { "const": "risk" },
    "status": { "type": "string", "enum": ["proposed", "confirmed", "superseded", "discarded", "disputed"] },
    "title": { "type": "string", "minLength": 1 },
    "date": { "type": "string", "format": "date" },
    "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
    "owner": { "type": "array", "items": { "type": "string" } },
    "mitigated_by": { "type": "array", "items": { "type": "string", "pattern": "^DEC-[0-9]{4}$" } },
    "evidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["source", "ref"],
        "properties": {
          "source": { "type": "string", "enum": ["meeting", "slack", "mail", "confluence", "notion", "document", "spreadsheet", "image", "code", "tracker", "manual"] },
          "ref": { "type": "string", "minLength": 1 },
          "locator": { "type": "string" }
        }
      }
    },
    "tags": { "type": "array", "items": { "type": "string" } },
    "confidence": { "type": "string", "enum": ["FACT", "INFERENCE", "UNKNOWN", "CONFLICT"] }
  }
}
